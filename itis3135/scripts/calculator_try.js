var x = window.prompt("Enter first number?");
    var y = window.prompt("Enter second number?");

function add(){
    addNum = +x + +y;
    alert(addNum);
}

function subtract(){
    subNum = x - y; 
    alert(subNum);
}

function multiply(){
    mulNum = x * y;
    alert(mulNum);
}
function divide(){
    divNum = x / y;
    alert(divNum);
}